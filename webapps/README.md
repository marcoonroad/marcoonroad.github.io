# Subdirectory for PWA apps

The `fakeapps` subdirectory is just a directory for dummy PWA apps, directed for security,
by using _fake mobile apps_, we can protect our data against the theft of the phone and potentially
malicious actors.
